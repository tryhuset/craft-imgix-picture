<picture{% if attr|default(false) %} {{attr}}{% endif %}>
    {% for source in sources %}
        <source srcset="{{ source.srcSet }}"{% if source.media|default(false) %} media="{{ source.media }}"{% endif %}{% if source.sizes|default(false) %} sizes="{{ source.sizes }}"{% endif %} />
    {% endfor %}
    {% if img %}
        <img src="{{ img.src }}"{% if img.srcSet %} srcset="{{ img.srcSet }}"{% endif %}{% if img.sizes %} sizes="{{ img.sizes }}"{% endif %} alt="{{ img.alt|default('') }}" />
    {% endif %}
</picture>
