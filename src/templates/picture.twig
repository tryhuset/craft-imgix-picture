<picture{% if class|default %} class="{{ class }}"{% endif %}>
    {% for source in sources %}
        <source {{ source|raw }} />
    {% endfor %}
    {% if img is defined %}
        <img {{ img|raw }} />
    {% endif %}
</picture>
