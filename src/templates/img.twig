<img{% if attr|default(false) %} {{attr|raw}}{% endif %}{% if img.srcSet|default(false) %} srcset="{{ img.srcSet }}"{% endif %}{% if img.src|default(false) %} src="{{ img.src }}"{% endif %}{% if img.sizes|default(false) %} sizes="{{ img.sizes }}"{% endif %} alt="{{ img.alt|default('') }}" />
